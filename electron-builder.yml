appId: com.boltberry.boltberry
productName: BoltBerry
copyright: Copyright © 2026 BoltBerry

directories:
  output: release
  buildResources: resources

files:
  - dist/**/*
  - node_modules/**/*
  - package.json
  - "!node_modules/**/{CHANGELOG.md,README.md,README,readme.md,readme}"
  - "!node_modules/**/{test,__tests__,tests,powered-test,example,examples}"
  - "!node_modules/.bin"
  - "!**/*.{iml,o,hprof,orig,pyc,pyo,rbc,swp,csproj,sln,xproj}"
  - "!**/*.map"
  - "!**/*.js.map"
  - "!src/**"
  - "!**/*.ts"

asarUnpack:
  # better-sqlite3 contains native binaries – must be outside asar
  - node_modules/better-sqlite3/**/*
  - node_modules/bindings/**/*
  - node_modules/file-uri-to-path/**/*
  - node_modules/archiver/**/*
  - node_modules/archiver-utils/**/*
  - node_modules/unzipper/**/*

# Bundle compendium PDFs (e.g. the D&D 5e SRD) under the app's resources path
# so they ship with the installer. Available at runtime via
# process.resourcesPath/compendium/*.pdf. Users can drop additional PDFs into
# their per-user compendium folder; the resource copy is treated as a
# read-only seed.
extraResources:
  - from: resources/compendium
    to: compendium
    filter: ["**/*.pdf"]

# ─── Windows ──────────────────────────────────────────────────────────────────
win:
  target:
    - target: nsis
      arch: [x64, ia32]
  icon: resources/icon.ico
  # Sign with signtool if certificate env vars are present
  # certificateFile: ${env.WIN_CERT_FILE}
  # certificatePassword: ${env.WIN_CERT_PASSWORD}

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  installerIcon: resources/icon.ico
  uninstallerIcon: resources/icon.ico
  installerHeaderIcon: resources/icon.ico
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: BoltBerry
  runAfterFinish: true

# ─── macOS ────────────────────────────────────────────────────────────────────
mac:
  target:
    - target: dmg
      arch: [x64, arm64]
    - target: zip
      arch: [x64, arm64]
  icon: resources/icon.icns
  category: public.app-category.entertainment
  hardenedRuntime: true
  gatekeeperAssess: false
  notarize: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist

dmg:
  icon: resources/icon.icns
  iconSize: 80
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications

# ─── Linux ───────────────────────────────────────────────────────────────────
linux:
  target:
    - target: AppImage
      arch: [x64]
    - target: deb
      arch: [x64]
  icon: resources/icons
  category: Game
  description: Lokale Virtual-Tabletop-App für Pen-and-Paper-Runden

# ─── Auto-Updater (GitHub Releases) ──────────────────────────────────────────
publish:
  provider: github
  owner: RollBerry-Studios
  repo: BoltBerry
  releaseType: release
